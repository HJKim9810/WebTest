# 정보처리기사 짜투리 학습 퀴즈 설계

## 목표

2026년 5월 21일 정보처리기사 필기 시험 전까지, 사용자가 일하는 중간중간 긴장도 낮게 반복 학습할 수 있는 웹 기반 퀴즈 도구를 만든다.

핵심은 예쁜 소개 페이지가 아니라 실제 학습에 도움이 되는 문제 반복, 즉시 피드백, 오답 복습, 과목별 약점 확인이다.

## 시험 기준

- 필기 과목: 5과목
- 문항 수: 과목당 20문항, 총 100문항
- 문제 형식: 객관식 4지 택일형
- 합격 기준: 과목당 40점 이상, 전 과목 평균 60점 이상

## 1차 구현 범위

- 문제은행: 과목당 40문제, 총 200문제
- 첫 화면: 모드 선택 대시보드
- 풀이 방식: 즉시 정답/오답/해설 표시
- 출제 방식: 문제 순서 랜덤, 선택지 순서 랜덤
- 저장 방식: 브라우저 localStorage
- 채점 방식: 과목별 점수, 평균, 과락 여부 표시

## 모드

### 실전 100문제

- 5과목에서 각각 20문제씩 랜덤 추출한다.
- 실제 시험처럼 총 100문제를 푼다.
- 즉시 피드백은 유지한다.
- 결과 화면에서 과목별 점수, 평균, 과락 여부, 합격 여부를 보여준다.

### 과목별 집중

- 선택한 과목에서 20문제를 랜덤 추출한다.
- 특정 과목 약점 보강에 사용한다.
- 결과 화면에서 해당 과목 점수와 오답 토픽을 보여준다.

### 랜덤 연습

- 전체 문제은행에서 20문제를 랜덤 추출한다.
- 업무 중간에 빠르게 많이 풀기 위한 기본 연습 모드다.

### 오답 복습

- localStorage에 저장된 오답 문제만 출제한다.
- 오답 문제를 다시 맞히면 오답 목록에서 제거한다.
- 오답이 없으면 대시보드에서 비활성 상태로 안내한다.

## 문제 데이터 구조

문제 데이터는 UI 코드와 분리해 `questions.js`에 둔다.

```js
{
  id: "db-001",
  subject: "database",
  subjectName: "데이터베이스 구축",
  topic: "정규화",
  difficulty: "basic",
  question: "정규화의 주된 목적은?",
  choices: ["데이터 중복 감소", "네트워크 속도 증가", "화면 디자인 개선", "컴파일 시간 단축"],
  answer: "데이터 중복 감소",
  explanation: "정규화는 데이터 중복과 이상 현상을 줄이기 위해 릴레이션을 체계적으로 분해하는 과정이다."
}
```

정답은 선택지 인덱스가 아니라 문자열로 저장한다. 선택지를 매번 섞어도 정답 판정이 안정적이기 때문이다.

## 문제 품질 기준

- 오답 선택지는 고정 문구를 재사용하지 않는다.
- 오답 선택지는 같은 과목 안의 다른 개념에서 가져와야 한다.
- 문제 유형은 최소 2가지를 섞는다.
  - 용어를 보고 올바른 설명을 고르는 문제
  - 설명을 보고 올바른 용어를 고르는 문제
- 선택지를 읽지 않아도 패턴으로 맞힐 수 있는 문제는 실패로 본다.
- 문제 수를 늘리는 것보다 헷갈릴 만한 같은 범주의 선택지를 만드는 것을 우선한다.
- 1차 문제은행은 자체 제작 개념 문제이므로 실제 기출 대체물이 아니라 반복 암기와 개념 구분용이다.

## 저장 정책

`localStorage` 키:

- `ipe_wrong_ids`: 오답 문제 ID 배열
- `ipe_stats`: 누적 풀이 수, 정답 수, 과목별 풀이/오답 수

서버, 로그인, 데이터베이스는 1차 범위에서 제외한다.

## 채점 정책

- 실전 100문제 모드에서는 과목별 20문제 기준으로 점수를 계산한다.
- 과목 점수 = 정답 수 / 풀이 수 * 100
- 과락 = 과목 점수 40점 미만
- 합격 = 전체 평균 60점 이상이고 과락 과목이 없음
- 즉시 피드백 모드이므로 중간에도 현재 정답률을 보여준다.

## 성공 기준

- 사용자가 첫 화면에서 목적에 맞는 학습 모드를 바로 선택할 수 있다.
- 문제를 풀 때마다 해설을 보고 바로 다음 문제로 넘어갈 수 있다.
- 같은 모드를 다시 시작하면 다른 문제 순서와 선택지 순서가 나온다.
- 오답이 브라우저에 저장되고, 다시 접속해도 복습할 수 있다.
- 실전 모드 결과가 실제 필기 합격 기준과 맞게 표시된다.

## 1차 제외 범위

- 실제 기출문제 무단 복제
- 로그인/서버 저장
- 타이머 강제 종료
- PDF 출력
- 실기 필답형 채점
